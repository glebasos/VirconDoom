# VirconDoom

DOOM (linuxdoom-1.10, shareware data) ported to the Vircon32 fantasy console.

- `PLAN.md` — roadmap and feasibility analysis
- `CLAUDE.md` — build/verify workflow and porting conventions
- `bash build.sh walls` — build the current milestone ROM into `bin/`
